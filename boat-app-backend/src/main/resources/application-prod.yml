spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

# SpringDoc / Swagger UI — disabled in production to prevent API exposure
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
